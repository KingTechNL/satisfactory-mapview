worker_processes auto;

error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    log_format main '$remote_addr - [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;
    disable_symlinks off;

    sendfile on;

    server {
        server_name ${USER_DOMAIN};
        listen 80;
        listen [::]:80;
        http2 on;

        # Path to the root of your installation
        root /var/www/html;

        add_header Access-Control-Allow-Headers "Access-Control-Allow-Origin";
        add_header Access-Control-Allow-Origin *;

        # Handle OPTIONS requests for CORS
        if ($request_method = OPTIONS) {
            return 200;
        }

        # Serve files from /var/www/html/saves directory
        location /saves {
            alias /var/www/html/saves;
            autoindex on; # Enables directory listing
        }

        location /saves/* {
            alias /var/www/html/saves;
            #alias /var/www/html/saves/latest.sav;
            add_header Access-Control-Allow-Headers "Access-Control-Allow-Origin";
            add_header Access-Control-Allow-Origin *;
        }

        # Redirect root URL to satisfactory-calculator
        location = / {
            return 301 https://satisfactory-calculator.com/en/interactive-map?url=${SCHEME}://${USER_DOMAIN}/saves/${SAVE_NAME};
        }
    }
}
